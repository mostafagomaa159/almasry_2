part of '../orders_imports.dart';

class OrderItemTile extends StatelessWidget {
  const OrderItemTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
