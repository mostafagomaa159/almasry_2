part of '../product_list_imports.dart';

class _ProductListLoading extends StatelessWidget {
  const _ProductListLoading();

  @override
  Widget build(BuildContext context) {
    return const Center(child: CircularProgressIndicator());
  }
}
