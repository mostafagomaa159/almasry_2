import 'package:flutter/material.dart';

class BlinkHomeScreen extends StatelessWidget {
  const BlinkHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Blink Home Screen'),
      ),
    );
  }
}
